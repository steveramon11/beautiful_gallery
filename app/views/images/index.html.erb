<!DOCTYPE html>
<html>
  <p id="notice"><%= notice %></p>

  <body>
    <h1>Gallery</h1>

    <p id=intro>Upload a new image or click on a thumbnail below to see more details!</p>
    <br>

    <div class="button">
      <%= link_to 'Upload image', new_image_path %>
    </div>
    <br>

    <div class="gallery">
      <% @images.each do |image| %>
        <%= link_to image_tag(url_for(image.picture), height: 200, width: 200), image %>
      <% end %>
    </div>
  </body>
</html>
